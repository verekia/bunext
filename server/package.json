{
  "name": "server",
  "private": true,
  "files": [
    "index.ts"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun --hot .",
    "build": "bun build ./index.ts --production --compile --minify --bytecode --outfile bin"
  },
  "dependencies": {
    "serve-handler": "6.1.7"
  },
  "devDependencies": {
    "@types/bun": "1.3.13",
    "@types/serve-handler": "6.1.4"
  }
}
