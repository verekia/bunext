name: bunext

services:
  db:
    image: postgres:16.3-alpine3.19
    container_name: bunext-db
    environment:
      POSTGRES_PASSWORD: password
    volumes:
      - ./.db:/var/lib/postgresql/data
    ports:
      - '5432:5432'
